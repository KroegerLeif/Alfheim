import { LedgerHistoryView } from "@/features/inventory/components/LedgerHistoryView";

/**
 * LedgerPage Entry
 * Renders the chronological physical stock audit log table.
 */
export default function LedgerPage() {
  return <LedgerHistoryView />;
}

{
  "name": "alfheim",
  "private": true,
  "scripts": {
    "build:theme": "pnpm --filter @alfheim/keycloak-theme build",
    "check-types": "pnpm -r exec tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10"
  }
}
